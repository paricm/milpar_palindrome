# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in milpar_palindrome.gemspec
git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gemspec

gem 'minitest-reporters', '1.2.0'